apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: skip job test
on:
  workflow_dispatch:
jobs:
  job-if:
    if: ${{ false }}
    steps:
      - run: echo "hello"
        uses: docker://golang:1.20.3-alpine3.17
        name: Say hello
        shell: sh
  step-if:
    steps:
      - run: echo "hello"
        uses: docker://golang:1.20.3-alpine3.17
        name: Say hello
        shell: sh
        if: ${{ false }}
