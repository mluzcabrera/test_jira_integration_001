apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Say hello
        with:
          version: ${{ cloudbees.version }}
          url: docker.io/myapp/artifact:${{cloudbees..version}}
          name: artifact
        uses: cloudbees-io/register-build-artifact@v2
